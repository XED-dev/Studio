{{!--
  index.hbs — inFactory Base Theme
  Ghost Fallback: wird geladen wenn home.hbs nicht existiert.
  HINWEIS: home.hbs wird vom inFactory CLI auto-generiert.
           Diese Datei nicht löschen — Ghost braucht sie als Fallback.
--}}
{{!> home}}
