{{!-- Section: Venue Grid — Raeumlichkeiten mit Bild, Name, Kapazitaet
     Verwendet: Pages mit Tag #venue (feature_image, title, custom_excerpt)
     Tokens: --color-primary, --color-accent, --color-warm-bg, --font-heading --}}

{{#get "pages" filter="tag:hash-venue" limit="6" include="tags"}}
  {{#if pages.length}}
  <section class="c-venue-grid" aria-label="Raeumlichkeiten">
    <div class="l-wrapper">
      <div class="c-venue-grid__items">
        {{#foreach pages}}
        <article class="c-venue-card">
          {{#if feature_image}}
          <div class="c-venue-card__image">
            <img src="{{img_url feature_image size="m"}}"
                 alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                 loading="lazy" />
          </div>
          {{/if}}
          <div class="c-venue-card__body">
            <h3 class="c-venue-card__title">
              <a href="{{url}}">{{title}}</a>
            </h3>
            {{#if custom_excerpt}}
            <p class="c-venue-card__capacity">{{custom_excerpt}}</p>
            {{/if}}
          </div>
        </article>
        {{/foreach}}
      </div>
    </div>
  </section>
  {{/if}}
{{/get}}
